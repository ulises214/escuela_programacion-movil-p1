import styled from 'styled-components';
export const NumCommentsStyles = styled.span`
  .content {
    color: #c00;
    display: inline-block;
    transition: opacity 0.3s ease-in;
    overflow: initial;
  }
`;
